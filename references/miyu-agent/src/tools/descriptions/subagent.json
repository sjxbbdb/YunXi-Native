{
  "name": "subagent",
  "display_name": "子代理",
  "description": "Delegate a task to a subagent with your full tool set. Use it when the work would flood this conversation with tool output, when independent pieces can run in parallel, or for a side investigation. Coding goes here too with dev=true. The subagent starts from a fresh context and returns one final message, so prompt must stand on its own: background, conclusions you already confirmed, the goal, and what it should return. Use background=true for long tasks; you are woken when its whole task finishes. It may delegate one level further, no deeper.",
  "parameters": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "Short label, used for progress display."
      },
      "prompt": {
        "type": "string",
        "description": "The complete task prompt. The subagent sees nothing of this conversation, so include the background, the constraints and what to report back."
      },
      "dev": {
        "type": "boolean",
        "description": "Development mode: the dev system prompt plus a minimal coding tool set. Turn it on for every coding task and put the whole job, files and directories included, in prompt."
      },
      "max_steps": {
        "type": "integer",
        "description": "Cap on tool-call steps. Unlimited by default."
      },
      "tier": {
        "type": "string",
        "enum": [
          "lite",
          "cheap",
          "standard",
          "flagship"
        ],
        "description": "Model tier by difficulty: lite for trivial lookups, cheap for simple tool work, standard (default) for regular multi-step work, flagship for hard reasoning. Every tier has the full tool set; an unconfigured one falls back to the main model."
      },
      "background": {
        "type": "boolean",
        "description": "Detach: returns a job_id at once, check it with job(action=status)."
      },
      "session_id": {
        "type": "string",
        "description": "Continue an existing subagent by the session id from a previous result, or by a background job_id: queued as a follow-up if it is still running, otherwise a new turn there."
      },
      "resume_id": {
        "type": "string",
        "description": "Resume a run that failed with a resume_id in its error."
      }
    },
    "required": [
      "description",
      "prompt"
    ]
  },
  "permission": "writes",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "agent"
  ],
  "timeout_seconds": 0
}
