{
  "name": "install_aur_package",
  "display_name": "安装 AUR 包",
  "description": "Install an AUR package. Needs a prior review and the user's explicit confirmation.",
  "parameters": {
    "type": "object",
    "properties": {
      "package": {
        "type": "string"
      },
      "user_confirmed": {
        "type": "boolean",
        "description": "true only when the user confirmed after seeing the review."
      }
    },
    "required": [
      "package",
      "user_confirmed"
    ]
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "arch"
  ]
}
