{
  "name": "archlinux_official_package_query",
  "display_name": "查询 Arch 官方包",
  "description": "Query the official Arch Linux package database.",
  "parameters": {
    "type": "object",
    "properties": {
      "package_name": {
        "type": "string"
      },
      "repo": {
        "type": "string",
        "description": "detail mode, e.g. core or extra."
      },
      "arch": {
        "type": "string",
        "description": "detail mode, default x86_64."
      },
      "mode": {
        "type": "string",
        "enum": [
          "auto",
          "search",
          "detail"
        ]
      }
    },
    "required": [
      "package_name"
    ]
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "arch"
  ]
}
