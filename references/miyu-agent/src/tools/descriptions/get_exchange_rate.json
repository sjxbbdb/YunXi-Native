{
  "name": "get_exchange_rate",
  "trust": "external",
  "display_name": "汇率查询",
  "description": "Query exchange rates between currencies.",
  "parameters": {
    "type": "object",
    "properties": {
      "base": {
        "type": "string",
        "description": "Base currency, e.g. USD or 美元."
      },
      "target": {
        "type": "string",
        "description": "Target currency, e.g. JPY or 日元."
      }
    },
    "required": [
      "base",
      "target"
    ]
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": [
    "utility"
  ]
}
