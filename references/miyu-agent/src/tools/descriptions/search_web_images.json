{
  "name": "search_web_images",
  "display_name": "搜索图片",
  "description": "Search web images across several sources in parallel.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string"
      },
      "count": {
        "type": "integer",
        "description": "Required. Exactly how many to return: match what the user asked for, and do not fall back to the configured cap."
      },
      "safe_search": {
        "type": "boolean",
        "description": "Defaults to the plugin configuration."
      }
    },
    "required": [
      "query",
      "count"
    ]
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": [
    "images"
  ]
}
