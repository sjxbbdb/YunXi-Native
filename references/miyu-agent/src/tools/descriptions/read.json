{
  "name": "read",
  "display_name": "读取",
  "description": "Read a UTF-8 text file with 1-based line paging, or list a directory page.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "File or directory path."
      },
      "offset": {
        "type": "integer",
        "description": "Start line, 1-based."
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of lines to read."
      }
    },
    "required": [
      "path"
    ]
  },
  "permission": "readonly",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "dev-read"
  ]
}
