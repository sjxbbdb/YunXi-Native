# 配置 TUI 吞掉窗口缩放事件

状态：2026-09-16 用户验收通过，授权分项提交并合并 main。

共享 `read_key_with_timeout` 只返回按键，窗口 Resize 被循环吞掉；调用方一直
等待按键，没有机会根据新尺寸重绘。真 PTY 从 110×32 缩到 78×24 后无新增帧，
正式用例修前在 main-smaller 报红。

输入处理抽到 `widgets/input.rs`，保留真实按键与 Resize 的区分。菜单与编辑器
将 Resize 作为无操作唤醒后重绘，原业务按键不变。“按任意键”与 ERROR 提示
直接消费区分后的输入，缩放只重绘、不能关闭；超时轮询共用固定预算。

已审计 57 处读键调用。配置测试 53 项通过、1 项原有 ignored；新增 3 项输入
回归覆盖 Resize/真实 Null 区别、超时预算和下一编辑键保留。
PTY 验证主菜单缩放、编辑文字/光标保留、两类提示不误关闭。

```bash
cargo test --lib config_tui::
python3 testkit/tui/config_resize.py --binary /absolute/path/to/miyu
```

修前：`/tmp/miyu-config-resize-cj2zd47u`。
修后：`/tmp/miyu-config-resize-gqb2rngt`。
