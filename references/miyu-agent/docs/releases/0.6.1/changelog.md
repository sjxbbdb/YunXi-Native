# Miyu 0.6.1 完整更新记录


## 重要更新

- **DEB 包的支持下限从 Ubuntu 25.10 降到 24.04 LTS**。以前 24.04 装不上，报的是 `libc6 (>= 2.41)` 依赖不满足——这个门槛不是发行版特性，而是**构建基座**定的：GNU 渠道在 Debian 13 的容器里构建（glibc 2.41），产物引用 2.41 的符号，于是包里只能声明 2.41。现在基座换成 `ubuntu:24.04`（glibc 2.39），包里的 `libc6` 下限也不再写死、跟着基座走。24.04 支持到 2029 年，是目前装机量最大的那一档。

  Debian 13、Ubuntu 26.04、Fedora、Arch 照旧，另外新增 **Linux Mint 22**（22.x 的基座正是 Ubuntu 24.04，但它自带软件源，DEB 的依赖是从 Mint 自己的镜像拉的，所以单独装一次证明）；容器安装验收的两个 Ubuntu 目标改成一头一尾（24.04 下限 + 26.04 最新），中间版本的 glibc 都在这两者之间。**0.6.1 的 DEB 就是在这个新基座上构建的**，并且在 Ubuntu 24.04、Ubuntu 26.04、Debian 13 与 **Linux Mint 22.3** 四个干净容器里各真装了一次。

- **引导里「自选功能」那张表，现在设置界面里能改了**，而且比引导那张更全。以前那一屏走完就再也没有入口——功能是跟着人格走的（存在人格自己的 `persona.toml` 里），可设置界面里一直只有「插件配置」这个**机器层**的菜单，两者对不上。

  主菜单的 **「插件配置」和「自定义提示词」合并成了一项「人格和功能」**：

  ```
  人格和功能              Miyu
  ├─ 当前人格             Miyu           切换 / 新建 / 删除 / 编辑别的人格
  ├─ 启用的功能           23 / 43        ← 就是引导里那张表
  ├─ 提示词与预设对话     内置 Miyu
  ├─ 防失忆提醒           启用
  ├─ 提醒间隔             5 轮
  ├─ 用户身份
  └─ 开发模式提示词
  ```

  「启用的功能」那张表**一行上有两个动作**，对应两层开关：**空格**是「这个人格用不用它」（写人格自己的 `persona.toml`），**回车**是「这台机器上它怎么配」（密钥、尺寸、账号——原来「插件配置」里的那些表单，现在挂在对应那一行上，行尾有个 ⚙ 的就能进）。

  **勾上一行会顺手把机器层的开关也打开**：两层都开着工具才挂得上，以前勾了却因为机器层关着而不生效，人得在两个菜单之间来回找。取消勾选只收回人格这一层，机器上的配置留着——下次勾回来不用重填。勾着但机器层关着的行会标「(本机未开)」。

  表比引导里那张多摆三类：**只有机器层的那两件**（网络搜索、识图——引导里当「能用的底线」藏着）、**长期记忆与技能**（dev 人格关掉的正是它们）、**常开的内置插件**（文件、用量查询、脚本工具、MCP…）。按子系统 / 内置插件 / 脚本 / 技能 / MCP 服务器分节。

  **改完跟着「保存并退出」一起落盘**，和设置界面其余部分一个口径：只改了功能表、直接按 q 退出，照样会问你「保存 / 不保存」，答不保存就整份丢掉。开发模式提示词那一屏也不再有「保存 / 返回」两个按钮——编辑完退出就行，同样跟着统一的保存走。

  ⚠️ 不用重启 daemon，但**人格清单的改动要她下一轮才读到**（工具面是每轮按清单构造的）。⚠️ 引导里勾选现在同样会打开机器层开关（同一个口径）。

- **```mermaid 围栏现在会画成图**，终端和 WebUI 都是。以前她画个流程图给你，你看到的是一坨 `flowchart TD` 源码。

  - **终端**（kitty / ghostty）：图直接画在回复里，跟着正文一起滚动、回翻、重画。其它终端交给 chafa（Konsole / WezTerm / foot / iTerm2 之流都能出真图）。哪条都走不通——终端不认图形协议、语法有硬错误、图型不支持——就照常显示那个带语法高亮的代码块，**永不阻断输出**（和行间公式一个规矩）。图按内容的自然尺寸画，只有横过正文区才等比缩；**竖着长的图不缩**，占几屏就几屏，往回翻得到。图下面有一行 **「点开看大图」**，点一下用系统看图器打开矢量原图。⚠️ 如果点开来的不是看图器（比如某个图片压缩工具），是你系统里 `image/svg+xml` 没指定过默认程序，`xdg-mime default <看图器>.desktop image/svg+xml` 定一次就好。
  - **WebUI**：图以**卡片**形式直接渲染在 AI 输出里。卡片右上角「源码」在图和源码之间切换，旁边是复制源码；点图放大走和图片一样的灯箱。卡片限了高，太高的图缩着放、工具栏会写「点开看原图」。画不出来的那张整块退回普通代码块，源码一字不少。
  - 两边共用**同一个纯 Rust 渲染器**（`mermaid-rs-renderer`，MIT，23 种图型），不再需要 mermaid.js：图在服务端渲染完给前端，浏览器零 CPU、零第三方脚本。中文标签正常。源码超过 64KB 不渲染。
  - **刷新页面不会重画**：服务端记着画过的图（同一张图第二次要只花 1 毫秒不到，原来每次都要重算——实测一张十来个节点的图每次 0.6 秒，一页两三张图刷新一下就是一秒多的「正在画图…」）。重启 Miyu 之后头一次打开那一页仍会现画一次。
  - 终端画过的图会**缓存到 `<家>/cache/diagrams/`**（留最近 128 张）。重开会话时终端会把最近几轮正文重放一遍，没有缓存的话每张图都要重新算一遍——实测一张二十来个节点的图单次要 130ms，三轮就是近半秒的白等；现在是 0.1ms。

## 重要更新（续）

- **WebUI 里「运行命令」那一步改成和终端一个样**：抬头是 `运行命令 · 耗时 · 这条命令在干什么`，**命令本身排在抬头底下**，不再挤在抬头右边看半截。露几行跟设置里的「命令显示行数」（默认 8 行，装不下时最后一行换成 `⋮`，完整命令点开看）。一条很长的单行命令会自动折行把这几行空间铺满，不再是一行加省略号。命令那几行**可以直接点开**（拖着选文字复制不会误触发），悬浮时整条一起亮。
  - 点开之后「参数」和「结果」有了标题和分界线，不再连成一坨分不清哪儿是命令、哪儿是输出。
  - 末尾那一步的命令旁边也有竖线了——以前只画到最后一个圆点，看起来「有线」的那根其实是下一步的。

## 修复

- **回合跑着时开 `/models` / `/session`，思考不再被切成好几行**。以前她正在输出（尤其是长思考）时敲 `/models` 或 `/session`，每开一次面板再 Esc 退出，屏幕上就多一行「Worked for … · 1 thought」：这两个面板原来是把这一轮先分离到后台、开完再挂回来看的，每分离一次渲染就断一段。现在它们直接嵌在回合里跑（和她反问时弹的提问面板同一条路），面板收掉接着画，同一段思考只收成一行。`/models` 选完当场落到这条会话上（下一轮生效，footer 的模型标签跟着换）；`/session` 挑了别的会话就切过去，这一轮在后台继续跑。面板开着的那几秒正文不往下流，收掉后一次补齐；回合在面板开着时说完也一样，收掉面板就看到结尾。`/effort` `/persona` 的面板仍走原来的分离路（选完要重载配置），回合中开它们还会留一道接缝。只改终端客户端，不用重启 daemon。

- **macOS 上四处修复**（Linux 侧行为一字未变）：
  - **fish 集成以前装了等于没装**。hook 被写到 `~/Library/Application Support/fish/conf.d/`，而 fish 只读 `~/.config/fish`（或 `$XDG_CONFIG_HOME`），所以 `miyu fish-init` 跑完毫无反应也不报错。现在按 fish 自己的规矩放。
  - **剪贴板读写**补上 `pbpaste` / `pbcopy`（内置的 `read_clipboard` 脚本早就分好平台了，Rust 这边一直没跟上，同一台机器上脚本读得到、Miyu 自己读不到）。只做纯文本，图片仍按「这台机器没有剪贴板后端」处理。
  - **认不出当前 shell 时退回 `$SHELL`**。原来只看 `/proc`，macOS 没有这个目录，装完 hook 那句「当前 shell 要不要重载一下」的判断彻底失效。
  - **删除守卫补上 macOS 的系统目录**（`/System`、`/Library`、`/Applications`、`/Users`、`/Volumes`），以前那张危险目录表只有 Linux 那一套。

- **思考内容那扇滚动小窗左边的竖线不再被滚走**。窗里的字往上滚时，那根线会跟着一起滚上去，窗口顶上空一截。

- **全屏 TUI 里点链接在 macOS 上一直没反应**。打开链接写死了 `xdg-open`，那是 Linux 桌面的命令，macOS 上根本没有。现在按平台取（macOS 用 `open`）。顺带：`file://` 开头的链接以前也点不开，只认 http/https。

## 修复

- **后台不再攒下一堆几 MB 的 `miyu` 僵进程**。起 daemon 的那个进程没了之后，daemon 现在会跟着退出（内核层面的 `PR_SET_PDEATHSIG`，启动者怎么死的都算，被强杀也算）。唯一的例外是 Miyu 自己启动的那只——`miyu daemon start` / 自动拉起的后台服务本来就该活过你的终端，它带着标记，不受影响。⚠️ 要重启 daemon。

  说明一下这些进程是哪来的：**不是**「换二进制重启 daemon 留下的」（那条路一直是好的，旧的会被正常收掉，实测确认过）。它们全部来自开发用的测试脚本——脚本被外层超时打断时，Python 的默认信号处理不会执行清理代码，脚本里那句「关掉 daemon」永远轮不到，沙箱 daemon 就被系统过继下去一直跑着。仓库里起 daemon 的测试脚本有 66 处，挨个改既改不完也挡不住强杀，所以这道保险放在 daemon 自己身上。

- **报错不再多一句「该怎么办」**。模型端点失败时末尾那行 `→ 等冷却结束，或换一个供应商`（以及认证失败的「检查一下 API key」、内容策略的「换个说法再发一次」等同类提示）全部去掉了。端点那一行里已经写着「被限流或额度用完（HTTP 429）」「该端点暂停 10 分钟」，再补一句等于把同一件事说两遍。现在的形状就是「一句结论 + 每个端点一行」，请求 id、供应商原话、冷却时长一个没少。⚠️ 要重启 daemon。

- **QQ 历史记录里看得见撤回了**。问她「刚才群里聊了什么」时，被撤回过的消息以前会凭空消失——不是显示成「已撤回」，而是整条不存在，连「这里有人撤回过」都看不出来。撤回其实一直在记（你库里 797 条撤回记录、795 条消息标着撤回时间），只是**查询默认把它们全滤掉了**，而且负责打「(recalled)」标记的那段渲染代码从上线起就没被执行过。现在 `search_real_chat_history` 会把撤回过的消息一起返回并标出来，正文照给——历史要的是「当时发生过什么」，有人把话收回去本身就是发生过的事。**被管理员撤掉的（不是本人收回的）会标成 `(recalled by qq=<操作者>)`**：你库里 794 条有主的撤回里，134 条是别人撤的，这和自己把话收回去是两回事；本人撤回就只标 `(recalled)`，不再重复一遍发送者的号。工具描述里也写明了这两种标记。⚠️ 要重启 daemon。

  口径说明：**她实时看到的群聊上下文不变**。那条路是增量注入的——撤回发生在她看见之前，那条就不进上下文（本来就该这样）；发生在她看见之后，那条已经在对话历史里，不会被改写。**好感度评分也不变**：撤回的话不算进对你的印象里。WebUI 的 QQ 数据页早就有撤回数、只看撤回的筛选和逐条撤回记录，不受影响。

- **目标（`/goal`）跑着的时候她提问，不再卡在「准备问题」上**。`/goal 试试用问问题工具随意问我一个问题` 之后，那一步会一直挂着「准备问题 · N s」往上数，怎么等都不动。原因是渲染这类回合的那条通道少接了一个事件：目标续轮是 Miyu 自己开的轮，终端靠「挂上去看」的方式跟随，而那条路的事件表里没有「模型要提问」这一项——面板不弹、没人回答，那一步就停在「工具名已经认出来、参数还在传」的显示上，直到回合内部 30 分钟的兜底超时才收场。现在这条事件和你自己发起的回合共用同一份处理：面板照常弹出来，回答完这一轮接着往下跑，问答也照常留在正文里。后台任务唤醒的回合里提问同样受益（同一条通道）。⚠️ 要重启 daemon。

- **opencode Zen 的免费模型又能用了**。09-19 起 Zen 给免费模型（`mimo-v2.5-free`、`big-pickle`、各种 `-free` 型号）加了一道「只能从 OpenCode 里用」的闸，第三方客户端一律 `403 · OpenCode's free tier can only be used from within OpenCode`，Miyu 也在内。实测下来这道闸看的是**请求体里的工具名**：要同时出现一个叫 `shell` 和一个叫 `read` 的工具才放行（外加流式、外加带 `x-opencode-*` 头——后两件 Miyu 本来就满足）。Miyu 的两件工具叫 `run_command` 和 `read_file`，一个都对不上。现在发往 Zen 系端点（`/zen/v1` 与 Console Go 的 `/zen/go/v1`）的请求会把这两个名字换成 Zen 认得的写法，模型回来再换回去——你在界面上看到的工具名不变，别的供应商一个字节都不动。工具面里没有这两件工具的场合（群友的受限工具面、好感度评分这类不带工具的轮次）会补一条同名占位声明，占位不会反向打开权限。另外**保温 ping 对 Zen 关掉了**：它是非流式的，打过去必然挨 403，而 403 会按认证失败把这家端点冷却十分钟——为省一次缓存 miss 把端点摁死十分钟不划算。⚠️ 这道闸是 opencode 服务端的策略，他们随时可能改判据；改了的话 Zen 免费模型会重新开始报 403，可以跑 `bun run testkit/opencode-zen/freetier_probe.js` 看是哪一条变了。

- QQ 群聊的**回复触发与好感度**这一轮改了六处，核心是两件事：窗口叠加时加分**叠加**而不是抢占，违规关键词**不再吃掉社交回复**。
  - **观察窗口有自己的开关了**（设置 → QQ 真实上下文 →「群聊发完消息后观察窗口」）。以前它跟着「自然续聊」走：想关续聊就必然把「她刚发完言、接下来一会儿都留意着」一起关掉，而界面上没有任何提示。⚠️ 原本靠关续聊来一并关掉观察窗口的配置，升级后观察窗口会重新打开，要单独关。
  - **窗口叠加时加分叠加**。她回完你之后，续聊窗口（只认你，15 秒）和观察窗口（认所有人，30 秒）同时开着；以前按「主触发」一档定死，只拿得到一份加分。现在成立几个条件就加几份，不封顶——她刚发完言时有人 @ 她，回复意愿本来就该更高（冷静机制在另一头压着）。归类（好感度算不算直接互动、日志标题）仍取更具体的那个。
  - **命中违规关键词不再抢断正在进行的对话**。以前你正跟她聊着、说了一句含关键词的话，这一轮就被降格成纯违规检查：不是真违规她就一声不吭，连续聊窗口都跟着断掉；而同一句话只要加个 @ 她就会正常回复。现在违规检查是一面并行的旗子——判官本来就在查违规，有社交触发时照常社交回复，只有「一个社交条件都没有」时才由它单独把判断拉起来。真判定违规时的行为不变（叫醒她、带着预检结论回话）。
  - **违规关键词表按真机实测砍了 22 个**。匹配是子串且 ASCII 不分大小写，于是 `OD` 会中在 `model`/`code` 里（在一个 Arch 技术群的 38878 条消息里命中 447 次）、`64` 中在 `x86_64`、`盒` 中在「沙盒」、`节点` 中在「背光节点」——抽样里一条真违规都没有。砍掉的是「实测命中过且全是误报」的那些（`OD` `64` `sb` `lsp` `ntr` `3p` `4p` `VPN` `节点` `渠道` `辅助` `隐私` `实名` `梯子` `翻墙` `主义` `广告` `盒` `药` `南梁` `群号`），零命中的短词留着当兜底。默认表 175 → 153 条，实测触发量降 88%（每 43 条消息触发一次 → 每 374 条）。**自定义过词表的用户不受影响**，想照着改可以跑 `python3 testkit/qq/keyword_audit.py` 看自己群里的命中情况。
  - **好感度终于会动了**。评分规则原来只有五档、而且明写「普通交流 0」，可群里绝大多数互动就是普通对话——实测 1834 次模型调用几乎全输出 0，187 个人的档案里 165 个停在初始值 10。现在换成九档，最常见的那一档给「0 到 +0.8」的余地，其余分档也细化（感谢夸奖 +1.5~+3.5、刷屏打断 -1~-3 等）。单次与每日上限、置信度门槛都没动。
  - **好感度不再静默失败**：置信度不够被丢弃时的日志从 `debug` 抬到 `info`（daemon 默认只记 error，原来这套系统对用户完全不可观测）。另外它读的群聊条数从硬编码 12 改成跟随「判断上下文窗口」——两者都是判断，没理由各用各的数。
  - **覆盖窗口从 5 秒延长到 7 秒**（`active_reply_supersede_window_seconds`）：发错了马上改，五秒常常来不及重打一句。顺带说明一下它的判据——只看「同一个人 + 在窗口内」，**不看上一轮是什么触发**，所以观察窗口触发的那条消息同样能被补发的消息覆盖，而且会保留原始触发标签（不会把路人的一句话记成「跟她直接互动」）。
  - 新增开发文档 `docs/qq-reply-pipeline.md`：全链路时序图、触发条件表、三个模型各自看到什么的对照表、配置项之间的隐式耦合清单。

- QQ 聊天记录查询（`search_real_chat_history`）：**工具描述现在写清了它能按时间范围查**。按天数（`days`）、按起止时间（`start_time` / `end_time`）这三个参数一直都在、也一直生效，但工具描述只有一句「Read persisted QQ chat history.」，能力一个字没提——于是问她「上周三群里聊了什么」，她只会去捞最近的一堆，看着就像不支持按时间查。描述改成明说四种用法（关键词 / 按发言人 / 按时间范围 / 跨会话，可叠加），上下文塞不下时那句提示也点名了时间参数。另外**一次拿够、别分几次查**：分页提示那句原文是「narrow the time range or raise limit」，她照着前半句做了——把一天切成四个时间窗、每段只要 100 条查四次，同一段上下文跟着进四次缓存。现在那句改成「把 limit 调大，在**一次**调用里要完，不要拆时间范围」，工具描述里也明说了。配套把条数放宽：**省略 `limit` 时给 500 条**（原来是给上限），**单次上限从 1000 抬到 2000**，`qq_message_history.history_safe_page_limit` 默认也从 500 改成 2000（设置页同步，范围 1–2000）。顺带澄清：并不存在「固定 200 条」这个限制——200 是活跃度排行工具的上限和 WebUI 数据统计页的单页上限，和这个查询无关。

- 模型调用失败时的报错**改成说人话**。以前是一行英文模板加一整坨供应商原始 JSON（`no LLM provider/model endpoint succeeded … upstream returned HTTP 429: {"error":{...}}`），最要紧的那件事——这是额度问题还是 key 问题——一个字都没有。现在的形状是「一句结论 + 每个端点一行 + 一句该怎么办」：
  ```
  ✗ 错误: 所有模型端点都没跑通（llm_1789745736487_0）：
    · brokeA / broke-model（key#1）：被限流或额度用完（HTTP 429）：Rate limit reached … Requested 512.…；该端点暂停 10 分钟
    · brokeB / broke-model（key#1）：被限流或额度用完（HTTP 429）：Rate limit reached … Requested 512.…；该端点暂停 10 分钟
    → 等冷却结束，或换一个供应商
  ```
  分七档说事（被限流/额度用完、认证失败、这个端点没有这个模型、端点不认这种请求、请求被拒、内容策略拦截、供应商报错），带上**该端点要停多久**（这个数以前只进日志），供应商自己那句话留着、外面那层 JSON 丢掉。所有端点栽在同一件事上时才给「该怎么办」那一句——一半限流一半认证失败时给哪句都是误导。另外 **claude-code / codex / antigravity 这三条 CLI 中转线不再谎报「HTTP 429」**：它们根本不发 HTTP 请求，那个状态码是按子进程输出的措辞编的，照着去查 base_url 和代理只会白费工夫；现在直接说「额度用完或被限流」「没登录或登录态失效」。TUI 里这段带错误图标、明细压暗缩进，WebUI 的错误通知条也跟着支持多行。

- 全屏 TUI 里改窗口大小，已经在屏上的正文现在会**跟着重新折行**。以前拉宽窗口，正文还按旧宽度断着、右边多出来的那片整个空着；收窄窗口更糟——超出新宽度的那一截被直接切掉，看着像内容丢了。现在拉宽会并回去重排、收窄会重新断，来回改宽度也不会掉字，折出来的宽度和新写进来的内容完全一致。只并「折出来的」那种换行：作者自己敲的空行、代码块、表格、图片都原样不动，正文续行的那两格装订边照旧。

- 会话钉的模型被供应商下架或改名之后，现在会**自动退回全局模型池**，不再把这条会话锁死。以前钉的那几条一条都对不上时，配置里好好的全局池会被这份失效的覆盖整个顶掉，于是这条会话连打开都打不开——普通模式的 `miyu` 直接报一句「Miyu core returned an invalid admin response」，什么线索都没有（09-18 真机：某会话钉着一个供应商已经删掉的模型）。现在：能用的那几条照钉；一条都对不上就退回全局池、并把这份失效的覆盖清掉（不清的话每一轮都要重新踩一次，`/models` 里也还显示着一个用不了的池），进 REPL 时会有一行暗色提示说清是哪一条没了。顺带：daemon 处理命令出错时会把原因回给客户端再断开，不再让所有故障都长成同一句「invalid admin response」。

- 子代理开的后台命令，完成报告现在发回子代理自己（原来后台任务记在父会话名下，报告发到父会话头上，而子代理早已退出、没人跟进）。

- 全屏 TUI 里 `/compact` 期间时间线状态行上 logo 左侧那个点阵转轮现在会转，文案是「正在压缩上下文」（原来正文只有一行静态提示，几十秒里像死了一样；回合中的自动压缩转轮上也改成写「正在压缩上下文」而不是「正在思考」），footer 的声波也一起动；压完 footer 的上下文读数当场刷新（原来只把数写进正文那一行，footer 要等下一轮结束才变）。

- `/session` 列表每行改成「模式 · 当前上下文 · 标题」，上下文是那条会话当前的数（12k 这种短写，不带模型窗口），口径和 footer 一样：daemon 每次算出某会话的上下文（回合收尾、压缩、撤销、切换）都记到会话上，列表直接读；升级前的老会话第一次打开列表时现算补上（一次最多补 8 条，多开几次就齐了），没跑过回合的显示 0。摘要不再进行，但搜索仍认它。

- `/compact` 之后可以 `/undo` 撤销压缩（原来 daemon 侧就支持，全屏 TUI 里现在会明说「已撤销上下文压缩」）：那块「上下文已压缩」从屏上截掉，前一轮对话留着，footer 读数回到压缩前。shell 无缝对话已经打到终端上的不动。

- WebUI 里 AI 输出的「加载中」动效改成：三角顺时针转一圈 → 聚合 → 展开成三角 → 逆时针转一圈 → 聚合 → 展开成三角，循环；「水平一行」和尾段的水平弹跳都去掉了。输入框那条匀速三点不动（本来就是另一套动效）。

- 混合模型池（会话或全局钉了不止一个模型）时，全屏 TUI 每轮回复末尾又有那一行暗色的「供应商 / 模型」了（前面空一行），重开 TUI 回放最近几轮时也会补上。设置里「Mixed 时显示本次供应商/模型」三档照旧：`interactive` 只在交互会话显示、`all` 连 shell 无缝对话/单次命令也显示、`off` 不显示。根因有两道：那一行原来只在直连 REPL（`MIYU_DIRECT=1`）里认「交互」这一档，默认的 daemon 远端 REPL 把它写死成非交互；而且「是不是混合」拿的是全局配置判的，你在会话里钉了两个模型、全局只挂一个时永远判成不混合。现在按会话的池判，交互会话经正文通道落到回复底下。

- 脚本头部契约里的空 `enum` 项不再漏进出站工具表：`query_moegirl` 的 `site` 枚举末尾那个空串删掉了；MCP 桥（claude-code / codex / antigravity 三条线）吐工具 schema 前统一做一层与方言无关的净化——空串和类型不符的枚举项剔掉、剔空了连键一起删、`default` 不在枚举里就剔掉，且只在 schema 位置递归（名字恰好叫 `enum` / `default` 的参数不受影响）。此前只有 antigravity 线的 gemini 整形会滤掉空串，claude-code 与 codex 的工具表里一直带着它（issue #45 / PR #46）。

- `/models` 里取消「继承全局模型池」时，那些因为继承而勾上的模型现在跟着一起取消，不用再逐个去掉；勾回继承则回到全局池那几个。继承着的时候直接勾/取消某个模型，等于开始自己钉一批：继承自动取消、全局池那几个留作起点。另外原来「只取消继承、别的不动」回车会被当成「未做修改」什么都不发生，现在会明说「取消了继承但一个模型都没勾，仍继承全局模型池」；「取消继承并钉住原来那批」也成了一次真实改动。行内和全屏两个菜单一个规矩。

- 全屏 TUI 里 `/undo` 之后画面跟着变了：撤掉的那一轮（你那句和她的回答）从正文上消失，前面的轮和往上翻的历史原样留着；footer 的上下文读数当场刷新（原来只改了内存里的数，要等下一轮结束才变）；撤到空会话就回大厅。做法是提交回显和回放时在正文缓冲里埋「这一轮从这儿开始」的标记，撤销时只截回到标记处，不是整段重画。`/pop` 之后 footer 同样当场刷新。inline REPL 擦不掉已经打出去的内容，照旧只提示。

- 后台任务的完成报告（以及 `/goal` 的续轮提示词）不再出现在上方向键翻出来的输入历史里。它们是 daemon 自己合成的「用户消息」，在库里和你敲的话长得一样；重画正文那条路早就按开头标签把它们画成 ⚙ 提示而不是用户气泡，输入历史那条路原来没认这个标签——换一次会话（`/session`、`/new`、Tab 换车道、`/config` 重载）之后上键就翻出整段报告原文。现在三处（产出、回放、历史）用同一份标签常量。

- 新增 `/dev` 与 `/normal`：`/dev` 等同 `miyu dev`，切到开发模式那条车道上的会话（车道指针指向的那条），`/normal` 切回普通模式那条车道；输入框竖条、banner、footer 一并换过去。它们换的是会话，不是把当前会话改成另一个模式，所以不受「空会话才能按 Tab 换车道」的限制。已经在那条车道上时只提示一句。直连 REPL（`MIYU_DIRECT=1`）只给提示。
- `/session` 的列表现在普通、开发两侧合并列出（每行本来就带「普通：/开发：」标签；管理面 `miyu session list`、WebUI 侧栏早就这么列），`/session <名字|序号>` 也按这份合并列表解析。当前模式的会话排前面、另一侧的排后面（各自仍按更新时间），序号也按这个顺序。选中另一侧的会话时，模式跟着会话走——此前会出现会话已经跑在开发人格上、屏幕上还是普通模式配色的自相矛盾状态。

- 全屏 TUI 与 shell 无缝对话的时间线上，脚本接口接进来的脚本现在显示头部里写的「显示名称」，不再是裸 id（WebUI 此前就对）。根因：脚本的显示名只在 daemon 里登记，TUI / `miyu "…"` 是另一个进程，那张表是空的；现在客户端从 daemon 的工具事件里把名字学过来，「加载：某脚本」那一步点名的脚本也一并带上（不然画那一步时脚本自己的事件还没到）。

- 全屏 TUI 内容多了之后不再越来越卡。根因：只要有块展开着（「展开思考内容」开着时每一段思考落地就自动展开），画每一行都要把缓冲里全部块走三遍，一屏三十几行 × 几百上千块，每帧上万次查找——量尺上 500 个展开块时一帧 27ms（debug 档），改成按帧缓存的视图索引后 0.3ms；展开着东西时行级缓存也不再整个失效，正在想的那一块每帧重取内容也不再整屏重画。点开一份几千行的 diff 本身只要十几毫秒，不是卡的来源。顺带把可展开内容的登记处上限从 4000 行抬到 16000 行：一份两三千行的 diff 一落地就占掉四分之三额度，此前会把别的块（含自动展开的思考、正在跑的那一步）静默挤掉。
- 修复回合进行中 PgUp 翻到顶、再 PgDn 翻回底之后视口不再跟着输出走、要到下一次提交才恢复的问题。按键那一刻算的「底」用的是上一帧的活动区高度，落帧时活动区变了（状态行冒出来/消失、过程收成一行）视口被夹到底，却没把「跟随」扶正，从此每帧夹回同一个位置。现在被夹到底就是到底，跟随自动恢复；已经在底了再按 PgDn 也不再整屏重画一遍（原来每按一次闪一下）。

- 正在想的时候，「展开思考内容」**关着**也看得到它在想什么了：「思考中 · N 词元 · Xs」抬头底下开一扇窗，滚着露最近几行正文（不再是抬头末尾那一截单行窥视），想完照旧收成一行「已思考 · N 词元 · Xs」留在时间线里、收进 `Worked for …`。全屏 TUI 主线、shell 无缝对话（含单次命令、daemon 回写）、WebUI 主线过程时间线三处一致（WebUI 的窗左侧带一根和过程时间线同列的细线）；子代理那一路（TUI 子代理浮层、WebUI 子代理子过程、后台任务面板）不开窗，仍是原来的单行刷新窥视。**开着**时不变（全屏出来就是展开态、shell 无缝对话边想边往下流、WebUI 展开着就直接看正文）。新增设置**「思考滚动显示行数」**（设置 → 显示，在「命令显示行数」上一项，默认 10；0 = 只留抬头和单行窥视），WebUI 设置页同样能改。

- 「展开思考内容」开着时，shell 无缝对话（以及单次命令、daemon 回写）里思考正文现在**边想边往下流**：转轮挂在「思考中 · N 词元 · Xs」抬头上不动，正文在它底下一行行长（从连线穿过去），想完抬头换成「已思考 · …」落地、正文在它底下，全文只印一遍。整段高过一屏时最上面的行滚进 scrollback（先是抬头，再按折好的行一行行滚），抬头滚出屏幕后正文行上就不再挂转轮，想完在末尾补一行「已思考 · N 词元 · Xs」。以前这个形态下只有抬头末尾那截窥视在变，正文要等想完才一次性冒出来——全屏 TUI 靠的是「出来就展开」的块，这个形态没有可点开的块，正文没有别的通道。顺带修了转轮那几行流式时一闪一闪：每帧裹进同步输出块（终端一次成帧），并且只重写变了的行、新行在末尾追加，不再每帧整片擦了重画；落地一步时「擦转轮 + 写正文」也在一个块里。

- 空会话大厅的星空与扫光不再在斜杠命令期间定格。以前 `/session`（以及同款面板的 `/effort` `/persona` `/models`）面板开着时动画整个停住，关掉才恢复；`/config` 退出后、`/session` 选定后等 daemon 重载配置或切换会话那几秒（真机上重载 MCP 要好几秒）大厅画着但一动不动。现在面板开着时照常走帧，命令等 daemon 的时候也照常走帧；会话视图不受影响。面板里按住 j/k 换行时扫光不再一顿一顿（节拍按时刻算，不按「等满 40ms 没按键」算）。
- 开着 kitty 光标拖尾、在大厅用输入法打字时，光标偶尔会从星空里某处「瞬移」回输入框。根因在 kitty：它给输入法预编辑定位时用的是**活光标**，而大厅每 40ms 重画一帧的那几毫秒里活光标正在星星格子上，预编辑事件正好落在这段就把光标画到那儿、下一帧再弹回来。改成先在同步块外把整帧算好、块里只剩写入，块时长从约 1.8ms 压到约 0.35ms（debug 构建实测中位数），撞上的概率随之降到原来的五分之一左右（kitty 侧不改就不会是零）。

- 修复多模态主模型下 `vision_analyze` 一次传多张图时图**没有真的交给模型**的问题（表现像「不用模型自己的多模态能力、换了别的模型在看」）。QQ 线一条消息带多张图、TUI/WebUI 里图和视频混批、以及子代理里，逐张分析各自把图寄存给主模型，汇总时却被当成纯文本拼在一起，回合循环认不出、一张也没附上；工具回执里还写着已内联，模型以为自己看过了。现在汇总时把逐张的寄存合成一次：附上的图列在 `media`，走视觉旁路转述或出错的条目列在 `analyses`。寄存表里因此滞留的图片字节也一并清掉。
- QQ 线 `vision_analyze` 的 6 次调用上限改成按**工具调用**计，不再按图计：以前一条消息发 7 张图、模型一次全交，第 7 张就报「call limit reached」。历史图拉取次数与总字节的限额不变。

- 全屏 TUI 里 `/effort`（原 `/variant`）、`/persona`、`/models` 三个菜单改走和 `/session` 同一套面板：空会话大厅里贴在提示行下方、与输入框左对齐，不再从第 0 列起笔画在星空上（`/effort` 与 `/persona` 此前在大厅里错位、盖住输入框那几行）；`/models` 此前是把整个大厅收掉、菜单孤零零出现在左上角，现在大厅留着。会话里三个面板都贴在正文底部，正文按面板高度让位，PageUp/PageDown 与滚轮照样翻正文，缩放窗口面板跟着走。顺带：`/persona` 的抬头此前写的是「选择模型」，改成「选择人格」。

- `/variant` 改名 `/effort`（codex / Claude Code 的叫法），`/variant`（opencode 的叫法）保留为别名，打哪个都一样：打 `/var` 时候选里出的是 `/variant`、并注明它等于 `/effort`；`/help` 与打 `/` 列出来的只有 `/effort`。命令行的 `miyu variant` 同样改成 `miyu effort`，旧名照用。

- 修复 `/effort`（原 `/variant`）列出的档位跟着**全局文本模型**走、而不是当前会话正在用的模型：会话用 `/models` 钉了别的模型时，全局模型只有 default 就只能看到一个 default，怎么都切不到会话模型的档位。现在按会话模型列档位、按会话模型存盘，下一轮生效。命令行 `miyu effort` 对终端集成会话同样按会话模型来。

- 斜杠命令的候选提示不再在「打全」那一刻消失。以前 `/sessio` 有提示、补上 `n` 反而没了，像是打错了一样；现在打全了它还在，并带上参数提示（`/session [name|index]`），接着打参数时也留着。Tab 补全不碰已经在打参数的行。

- 新增设置**「不自动收起过程」**（设置 → 显示，默认关）。开了之后全屏 TUI 不再把过程收成 `Worked for …`，每一步就地留在屏上——和 shell 无缝对话那条路一个样子，但每一步仍然点得开。

- 耗时读数加了毫秒档。以前不到十分之一秒的步骤**什么耗时都不显示**（看着像"这步没花时间"），刚过线的又显示成没有信息量的 `0.0s`；现在统一成 `<1ms` / `340ms` / `2.5s` / `1m 15s`，和思考那一行的转轮读数是同一套。

- 命令那一步改版：抬头现在写「这条命令是来干嘛的」，底下印**命令本身**。以前抬头右边是命令的第一行、底下印几行输出；现在 AI 每次跑命令都必须给一个短标题（比如「重建二进制」），看不懂命令的人也知道它在做什么，而命令全文占满底下那几行——跑了什么才是要紧的，输出点开那一步就能看到完整的命令加输出。命令超过设定行数时底部显示 `⋮ 已省略`。原来的设置「命令输出显示行数」改叫**「命令显示行数」**，默认从 10 改成 8（你自己设过的数字不受影响）。全屏、shell 无缝对话、子代理浮层三处一致。
  - 代价说在前面：shell 无缝对话那种形态没有「点开」，所以命令输出在那儿看不到了。

- 编辑文件那一步的抬头加上改动量：`编辑文件 · /path/to/file.py · +12 -3`，加绿减红。一个补丁改多个文件时是合计。子代理浮层里同样显示。

- 全屏 TUI 与 shellhook 里，AI 写完任务清单之后的表格终于出现在该出现的地方。以前全屏下它要等整轮过程跑完才打出来，而 shellhook 那边它被直接插进还在继续的步骤中间、把左边那根竖线劈成两截。现在「写清单」被当作一段过程的句点：这一段当场收成 `Worked for …`，表格紧跟其后，后面的工具属于新的一段——和控制台里一直以来的样子对齐了。顺带把空行摆正：清单那一行和表格之间不再空一行，空行改留在表格后面。

- 修复空会话里**第一句话以路径开头**时 TUI 画面错乱：大厅不退场，AI 的回复直接画在它上面糊成一片，整轮说完了你看到的还是大厅，得再发一条才进对话视图。原因是判断「这是不是斜杠命令」只看了首字符，`/home/x/某文件.md 帮我看看` 这种以绝对路径开头的正常消息被误判成命令，于是跳过了大厅退场——可它照样发给了模型。现在按命令表判：真命令（`/config`、`/session` 等）仍然留在大厅，命不中的一律当消息，大厅照常退场。

- 她不能再用 `rm` 删东西了。以前 `run_command` 只有一份「命令拒绝子串」名单把关，默认只拦 `rm -rf /` 和 `rm -rf ~` 两条字面量，`rm -f 某文件` 从来没被拦过；而且是纯子串匹配，`rm -rf /tmp/build` 会被误杀（因为含 `rm -rf /`），`rm -fr /`、`rm  -rf /` 反而放行。现在改按**命令位**判断：`cd /x && rm -f y`、`sudo rm`、`ls | xargs rm`、`bash -c "rm y"`、`find . -exec rm {} \;` 一律拒绝并告诉她换个能挽回的办法（普通模式下就是移入回收站的「移入回收站」工具）；`git rm`、`docker rm`、`npm rm`、`rmdir` 这些 `rm` 不在命令位的写法照常放行。新增开关「高危命令拦截」（设置 → 工具，默认开），关掉就回到只有子串名单的老行为。

- 修复用 Antigravity（agy）时，QQ 群里她对**某些人**的消息长时间一句不回的问题。agy 会把执行错误记在整条会话上（额度用尽、它自己的上下文压缩失败等），此后每次续传这条会话都原样回吐当初那句报错，哪怕这一轮实际已经正常生成了回复——回复会被整个丢掉。群里非管理员的消息单独走一条会话，所以表现成「只有别人说话她装死」；实测一条坏会话粘了 1 小时 45 分、连吃 37 次调用。现在这一轮照常发出，同时把这条坏会话退休，下一轮自动换一条干净的；真失败（且不是额度／登录／策略这类换会话也没用的原因）也会立刻换一条重试一次。
- agy 线的用量统计改回本轮口径。此前记的是 agy 给的**整条会话累计**，QQ 单轮的 prompt 会虚高到几十万。
- 中转线（claude-code / codex / agy）的 CLI 进程超时后先请它自己收尾，5 秒没退再强杀；此前一律直接强杀。代价是超时报错最多晚 5 秒。
- 修复 QQ 覆盖窗口内旧消息尚在排队或取图时仍继续执行，以及旧回合收尾误清理新回复承诺的问题；窗口外独立请求和活跃回合接管保持原行为。
- MCP 的 `isError: true` 正确转换为工具失败，保留原报错内容，不再误判成功；成功结果输出与旧历史回放保持兼容。
- 配置 TUI 在窗口缩放后立即重绘，保留编辑文字与光标；缩放不会被当作“按任意键”关闭提示，输入轮询也不会重置超时预算。
- 全屏 `/session` 在空会话保留大厅并显示于提示下方，在已有会话按面板高度预留正文空间，支持滚动、缩放、搜索与删除；选择当前会话不再重新加载或丢失阅读位置。
- 全屏 TUI 的短通知在 AI 回复期间也会按期消失，不再等到回复结束才清理；daemon 与直连模式一致。
- 全屏 TUI 的流式正文、输入框、状态行和光标归位纳入同一同步绘制事务，避免 herdr 等终端复用器看到中间光标位置。同步事务支持嵌套及异常收尾。
- TUI 提问面板按实际高度为正文让出空间，并与正文保持一行空白；提问期间仍可查看回复末段并回翻全部上文，切题、缩放和回答后恢复布局，直连与 daemon 路径一致。
- 修复窄终端/SSH 下运行动画导致 footer 换行、溢出片段在后台任务区闪烁的问题；宽度计算兼容中文与 emoji，极窄窗口也保持单行。

- QQ 群里**她的引用时灵时不灵**，现在这种情况不会再悄无声息了。症状是：该引用的消息发出去只有 @、没有引用气泡，而我们这边一切正常——日志说发送成功、聊天记录里也记着引用。真相是引用被 QQ 那侧的 OneBot 实现**默默扔掉**了：它给我们的消息号是「有符号 32 位整数」，天生一半是负数，可它自己收引用时又拒收负数号，于是**每两条引用就有一条**在群里看不见（09-19 真机实测：43 条引用里正数号 22 条全部正常、负数号 21 条全军覆没）。这是对端的问题，SnowLuma 已在 **v1.14.9** 修好（`fix(onebot): resolve signed-int32 reply ids`），**用 1.14.9 以下版本的请升级**；NapCat 较新版本不受影响（它把号的符号位抹掉了，只发正数）。Miyu 这侧新增了三样东西，让这类"发出去了却没生效"的事再也藏不住：每条带引用的消息发完会回头核对一次引用还在不在，不在就在日志里报警并点名是哪条；引用/艾特的判定依据（隔了几条别人的消息、你设的门槛是多少、上一条是不是她自己发的）也一并记下来，以后 `和原消息间隔几条消息则引用` 这类设置有没有生效，翻一行日志就知道。

- 她正在**滚动思考**的时候点开那一步，屏幕不再"鬼畜"了。原来一点开，展开的思考正文和底下那扇滚动窗会**同时**渲染——同一段话两份，还各自跟着流在长，行数每帧乱跳。根子在一个为了不闪而做的优化：转轮每帧只重写变了的那几行，而滚动窗的第一行（转轮 + 秒数）每帧都在变、后面几行常常没变，于是那一帧只发出了这一块的"开始"记号、没发"结束"记号；缓冲把这一块当成"长度未知"，展开时就把内容**插进去**而不是替换掉那几行。现在原地重写会保住这一块原来的范围，内容长了范围也跟着长。

- **自己点开的那一行，不会再被自动收起来了。** 以前点开正在想的那一步，想完就合上；一段过程跑完收成 `Worked for …` 时，里面点开着的步也一起收没。现在「你亲手点开的」优先于「默认怎么显示」：想完/跑完那一步还开着，这一段收尾时的 `Worked for …` 也直接是展开的。开着「展开思考内容 / 展开工具调用信息」的人不受影响——默认开着和亲手点开是分开记的，不然每一段都会得到一个摊开的收缩行。

- **终端里发表情包的那几行排对了。** 以前每发一次表情包都会多出两行红色的「表情包 · 已中断」，而那一次其实是成功的——发图前要先请渲染器把终端让出来，那一刻这次调用还没返回，于是被当成「没跑完 = 中断」记一次，真结果回来时又记了一次。另外顺序也反了：图是工具一开头就交出来的，当场打的话「表情包 · 67ms」那一行反而排到图下面，读起来不像「用了表情包工具，于是图出来了」。现在图会等这一步落地之后再打，顺序是「表情包那一行 → 图」。全屏 TUI 里图和上下文之间的留白也摆正了：上面空一行（以前紧贴着 `Worked for …`）、下面也只空一行（以前空两行），任务清单表同理。

- **shell 集成现在会跟着 Miyu 一起升级了。** 生成的 hook 文件顶上多了一行
  `# miyu shell hook · v0.6.0 · 2ff5d8cf`（版本号 + 内容指纹），升级之后随便跑一条
  miyu 命令，装着的那份就会自动换成新的——不用再想起来手动跑一次 `fish-init`，新版的
  补全、按键绑定、拦截逻辑下次开终端就生效。fish / bash / zsh 三个都有。
  几条保险：只刷新**已经装过**的文件（没装 shell 集成的人不会被自作主张装上，
  `.bashrc` / `.zshrc` 也一个字不碰）；换之前先让那个 shell 自己 `-n` 解析一遍，
  过不了就留着老的；那个名字下放着不是 Miyu 生成的文件时一个字不动；把 hook 链进
  dotfiles 仓库的，换的是链接指向的那份、链接本身还在；机器上放着更新版本装的 hook 时，
  旧二进制不会把它拽回去。设了 `MIYU_HOME` 的（沙箱、测试）默认不做这件事。

- **同一个会话可以开两个终端界面了，两边看到的是同一份实况。** 以前在 AI 输出到一半时
  再开一个 TUI 进同一个会话，看到的是**一片空白**——会话不空所以不进大厅，而正在跑的
  那一轮又不会被回放。现在第二个界面会自动挂到正在跑的那一轮上，把**已经流过去的那半截
  从头补一遍**（用户那句话、思考、工具步骤、正文全有），然后接着实时流，两边同一时刻
  看到的内容是一样的。
  同一个会话再发消息也不再各跑各的了：消息会**排进正在进行的那一轮**，AI 在工具边界
  取走它接着答，两边都看得见这条排队。第二个终端里直接说自然语言（shell 集成）同理，
  会打一行「已排进正在进行的对话」，然后从当下接着显示那一轮的回答。
  注意 TUI 和 shell 集成用的**不是**同一条会话（前者是按人格的界面会话，后者是终端会话），
  所以「开着 TUI 时在别的终端说话」本来就互不打扰，这次改的是同一条会话上的多个界面。

- **点开命令那一步之后，命令本身有语法高亮了。** 她自己写一段脚本跑（`python3 - <<'PY'`
  里塞几十行 Python 那种）时，展开来看以前是一片单色。现在关键字、字符串、数字、注释
  分色，而且 **heredoc 里的内容按解释器认语言**——`python3 <<'PY'` 里按 Python 着色，
  外面那层 shell 还是 shell。
  两个限定：只有**多行**命令才着色（`ls -la` 这种一行的仍然是暗色——逐词上色只是整行
  变亮，没有信息增益）；认不出语言的 heredoc 不猜（`cat > 文件 <<'EOF'` 里装的是数据，
  维持暗色）。命令的**输出**不着色，照旧。
  顺带：代码高亮里 Python 的 `True` / `False` 现在和 `None` 一样按常量着色了（以前只有
  小写的 `true` / `false`），正文里的 markdown 代码块也跟着受益。

## 重要更新

- **设置界面整个重画了**（`miyu config`，以及全屏 TUI 里的 `/config`），跟第一次启动的引导界面是同一张脸：屏顶是渐变的 MIYU 艺术字，两侧铺着会闪的星空，banner 上有一道来回扫的光；居中的那个 `┌─ MIYU 配置 ─┐` 框没有了。自己在 `config/banner.txt` 换过艺术字的话，这里也跟着换（和空会话大厅读的是同一份）。

  - **每一行拆成「名字 + 当前值」两列**。以前是 `配置全局文本模型 (当前: Stub / stub-model)` 挤在一行，现在名字在左、当前值在右列对齐，一眼能扫完一屏；表单（全局设置、插件设置、各家供应商表单）同理，值都在同一列起笔。
  - **知道自己在第几层**。banner 下面那行是面包屑：`● 配置 ── ● 接入通讯平台 ── ◉ 命令列表`，走过的层是实心点、当前层是空心点，和引导里的进度轨是同一套画法。
  - **按键提示挪到屏底**，成了「键 + 说明」的样子（`↑↓ jk 移动   ⏎ 选择   Esc 返回`），上面有一道渐变细线隔开。**一行放不下会折到第二行**——以前一行列不下时被截掉的，总是排在最后的那个「返回」。右下角多了 `3/18` 这样的位置计数。
  - **插件总表成了引导里那张功能表的样子**：`[*] 表情包        人格表情库与发送尺寸`，开关、名字、说明三段对齐。
  - **供应商 / 组织 / 模型那三列**也搬了过来：表头钉在上面不跟着滚，当前列亮、旁边两列暗，选中行在哪一列都看得见。
  - **正文和底下那行状态/报错之间空一行**，不再贴在一起分不清哪句是列表的最后一项；右列的值也从最暗那档（那是给补充说明用的）提了一档，深色背景下读得清。
  - **整块永远在屏幕正中**，不管这一层有几个条目：条目少块就矮、按键条跟着贴上来，条目多块就长到屏幕吃得下为止、再多的滚动。大窗口下不会再出现「列表贴着屏底、顶上空一大片」。
  - **底下那行报错会换行，而且是暖红的**。拉模型失败时那一长串（`401 Unauthorized: {"error": ...}`）以前直接冲出屏幕右边、颜色还和「已激活模型」这类普通提示一样是金色；现在按正文宽折行，报错用暖红、说一声用金色，一眼分得开。
  - **换层是一次画完的**，没有逐行铺开的动画——设置界面是来回切菜单的地方，每进一层都等它铺开反而碍事。星空和扫光照旧在动，而且按住 j/k 连着切也不会把它们顶停。
  - **窗口矮于 30 行时 banner 自动收成一行**（只留 `M I Y U ─ A G E N T` 和那道扫光），正文不会被装饰挤掉；窗口拉大再长回来。改窗口大小时正在编辑的内容、弹着的提示框都照旧保留。

  **按键一个都没变**：`j/k` 与 `↑↓` 都能走，`q` 与 `Esc` 都能退，`Enter` 进入 / 编辑，改动仍然是退出时一起保存。⚠️ 不用重启 daemon——设置界面画在你这边的进程里，装上新二进制直接生效。界面开着时会有一点持续占用（星空和扫光是 30ms 一帧画出来的），关掉界面就没有了。

- **新增 CodeBuddy 供应商**，和 Claude Code / Codex / Antigravity 一样，借你本机 `codebuddy` CLI 已有的腾讯登录态说话，Miyu 不经手任何凭据。首次引导（OOBE）探到装了 `codebuddy` 就会把它列出来，配置 TUI 里也有专属表单（可执行文件路径、原生工具作用域、Miyu 工具挂桥作用域、权限模式、流空闲看门狗），手写配置的话是 `protocol: "codebuddy"` 加 `plugins.codebuddy`。

  模型清单**从 `codebuddy -h` 现场解析**——它把支持的模型写在 `--model` 的说明里（`hy4-preview-f`、`hy3`、`glm-5.3`、`deepseek-v4.1-flash`、`kimi-k3-1` 等 16 个），所以 CodeBuddy 以后加模型不用等 Miyu 更新；万一它改了措辞解析不出来，就退回内置的那份清单。

  **供应商列表的次序也顺手理了**：本机 CLI 中转（Antigravity / Claude Code / CodeBuddy / Codex）现在恒定排在列表最前，彼此按名字的字母序，其余供应商的相对次序一个不动。以前是「Claude Code 置顶、Antigravity 和 Codex 紧随」的硬编码，每加一条 CLI 线都要改一次，而且新加的那条在**已有配置**里会被追加到列表最末尾（CodeBuddy 刚加进来时就掉在了最后一行）。

  两点和 Claude Code 不同：它的 CLI **没有思考档开关**，所以模型菜单里不会给这条线摆思考档位；**没有一次性会话选项**，辅助请求也会在它那边留下转录。原生工具、Miyu 工具挂 MCP 桥、多轮续传（只发增量不重发整段）都和另外三条线一致。

- 子代理会话化（第一段，后端）：每个子代理现在是挂在父会话下的一条真会话（`kind=subagent`），跑的是普通回合、历史落库；子代理可以再开一层「孙代理」，孙代理不能再往下开（工具面上没有 `subagent`，调用也会被拒）。「任务完成」的定义改为：子会话没有活动回合、且名下没有未完成的后台任务（后台命令、后台孙代理都算）——子代理起了后台命令再结束回合会进「等待」，命令跑完唤醒它、它再跑完、名下空了，父会话才拿到最终结论。停止父回合会级联停掉前台的子/孙代理，后台的子树继续跑、完成照常唤醒。daemon 重启时上次没跑完的子代理标成「已中断」，不自动续跑；给它回复就接着跑（`miyu ask --session <子会话 id>`，或 `subagent` 工具新增的 `session_id` 参数：跑着就排成 follow-up、闲着就起新一轮）。子代理会话只在主会话 reset 或删除时随树清理（删会话现在也会停掉它的后台任务、收掉中转进程），原来「审计会话 7 天自动回收」退役。子/孙代理的消耗计进统计账本，主会话的累计 Σ 含整棵树。`tier` 档位改为落在子会话的模型池覆盖上；dev 子代理就是 dev 人格的会话，提示词与 dev 会话同源。主会话的后台任务条现在把子代理、孙代理开的后台任务也列出来（前面挂 ↳），点开能看它们各自的过程；任务条的行鼠标悬浮会提亮（去 dim），和正文里可点的块一个规矩；WebUI 任务条现在也认开发模式的子代理任务（原来 `kind=dev` 的后台子代理被当成命令画：标签写「命令」、点开是原始日志而不是子过程时间线）；开发模式会话开出的子代理不管传没传 `dev` 都是开发模式（人格跟父），任务条与状态行的标签按实际模式写。子会话的系统提示词末尾多一句约定：回话对象是父回合、没有第二轮、后台任务（命令与子代理）完成会自动唤醒不要轮询。这一段只做后端，界面仍是原来的状态行 / 浮层 / 子过程时间线（daemon 把子会话的事件折回原标记喂过来）；「切进子会话看和聊」、`/subagent` 面板、任务条「↑ 主会话」在下一段。需要 `miyu daemon restart`。

- 「Miyu 回复完成」「Miyu 在等你回答」那条桌面通知现在**会响一声**，在 kitty 里还能**点通知跳回发它的那个窗口**。以前它由 `notify-send` 发出去就不管了：没有声音，点了也没反应（Wayland 下外部进程抢不了焦点，通知里也没带任何能反查「是哪个终端」的信息）。现在在 kitty（`TERM=xterm-kitty`）里改由终端自己弹这条通知——点它 kitty 会把自己那扇窗、那个标签页拉到前面。提示音用的是 Miyu 内置的那声木琴（上行两音，本来是给语音功能做的那套，现在进了默认构建，首次用时落到 `<家>/cache/sounds/`）——回复完成和她提问等你回答都是这一声。别的终端仍走 `notify-send`，声音一样有，点击跳窗做不到。新增设置**「通知带提示音」**（设置 → 通知，默认开，WebUI 设置页同样能改），关掉只是静音，通知照发；**「提示音文件」**填自己的音频文件就换成那一声（`~/` 会展开，wav/ogg/flac；想用系统声音主题就直接指到 `/usr/share/sounds/freedesktop/stereo/complete.oga` 这种文件；路径写错会退回内置音而不是变哑），**「提问时的提示音文件」**可以给「她在等你回答」单配一个，留空就和上面那个一样。顺带：kitty 里「窗口有焦点时不弹」改成由 kitty 自己判（它连「在非当前标签页」都算得准），以前终端不上报焦点时通知会一声不响，现在照弹。要在 WebUI 设置页看到新开关需重启 daemon。

- 表情包搜索一次最多返回的条数默认改成 5（原来默认 1、上限 3，模型常常要连搜好几次，每次都重走一遍前缀）；上限放到 10，配置 TUI、WebUI 设置页、配置校验三处同步。已经手改过这一项的配置文件不会自动变，请自己改成 5。

- Antigravity（agy）中转线现在**同一会话连续几轮复用一个 agy 进程**，省掉每轮约 5 秒的冷启动（起进程、登录、挂会话；09-16 真机统计短回合里冷启动占整轮 95%）。只在同一条会话、工具面/人格/模型都没变时复用；人格文件改了、换了模型或思考档位、群里管理员和群友交替发言换了工具面，都会换新进程。常驻进程闲置 10 分钟回收（设置 → Antigravity「常驻进程闲置回收」可调），删除/清空会话、重载配置、daemon 关停都会把它收掉；上一轮留下会话级 ERROR 的进程不复用。「同会话连续轮复用 agy 进程」开关默认开，关掉就回到每轮一个新进程。提示词里那句「每轮一个新进程」改成了「进程可能跨轮常驻但随时可能被换掉」，改动会让现有 agy 会话下一轮全量重放一次。

- 现在可以让她**把消息发到任意好友或群**（普通模式）：在 QQ 里或终端里说「把这个发到 xxx 交流群」「给某某发一句」，她用 `send_qq_message` 工具按名字（群名、好友备注/昵称、管理员别名）或号码投递，名字对不上会先用新的 `qq_contacts` 查地址簿（机器人的好友列表和所在的群），撞到多个同名的会把候选连号码报回来让她再确认，不会乱发。开发模式只保留 `send_qq_message` 且只能发给管理员（写代码时「跑完把结果发我手机」够用），没有地址簿工具。QQ 里这两个工具所有触发者都有（发消息是基础能力，也免得工具面按人分脸掰断缓存）；发回当前会话仍走原来的「发送消息」。两边都只在 NapCat 连着时出现，断开就收起。此前 QQ 里没有任何带收件人的发送工具，只能拿 `run_command` 直接打 NapCat 的 HTTP 接口。

- 新增全局设置**「终端集成会话默认模式」**（设置 → 显示 最后一项；WebUI 设置页同位置），可选「普通 / 开发」，默认普通。它管的是终端那条路——shell 无缝对话落进去的「终端集成会话」和裸 `miyu "…"` 单次命令——跑哪个模式：选开发后 shell 提示符敲的话走开发模式（工具面、提示词都换），「终端集成会话」在 `miyu session list`、`/session` 列表和 WebUI 侧栏里也标成开发；此前换过人格、终端那条路停在别的普通会话上的，会被拉回「终端集成会话」。改完在设置界面保存或 `miyu reload` 即生效，不用重启 daemon；daemon 启动时也会按它对一次。别的会话不受影响，改回普通会把这条会话换回当前激活人格。程序驱动的 `miyu ask --output-format json` 有自己的 `--mode`，不跟这个设置；TUI 里的普通/开发两条车道也不受它影响。

- 语音协议那一段只在语音功能开着时才进系统提示词：机器没开语音唤醒也没开播报，或者这个人格关了语音，就不再每轮多带它。升级后语音关着的会话第一轮缓存会冷启动一次，之后每轮少一小段；语音开着的一切照旧。

- 首次引导与人格设置页多了「防失忆提醒」「情绪」两个开关（机器侧没打开的功能不会摆出来）。首次引导的功能屏最后还多了一组「MCP 服务器」，配置里开着的每台服务器逐个勾，勾选结果写进这个人格的 `persona.toml`，不勾的服务器对这个人格连拉都不拉起。

- 脚本和 MCP 服务器可以查宿主信息了：脚本头部写 `Capabilities: host.info, providers.read, subsystems.read`（只对属主可信脚本生效），脚本里执行 `miyu host <方法>` 就拿到脱敏后的供应商摘要、契约版本、子系统开关；MCP 服务器在 `mcp.servers[].capabilities` 里声明同款。`miyu pm` 装扩展前会按 `requires-contracts` / `requires-capabilities` 预检。写法见 `docs/interfaces/host-capabilities.md`。

- **macOS 兼容修复**：语音输入、embedding / 渲染 worker、家目录迁移，以及 `hotel-deals`、`procusage`、`query_moegirl`、`read_clipboard` 四个内置脚本在 macOS 上的问题。一批只在 Linux 上成立的限制（内存上限、Landlock 沙盒）不再误伤 macOS。感谢 @yxxbc。

- 首次引导（`miyu oobe`）选功能那一屏，以及控制台里对应的那一页，都支持 **Ctrl+A 全开 / 全关**。还有没开的就全开上，已经全开了再按才是全关。

- QQ 的「私聊/群聊专属配置」多了一条**忽略睡眠时间**。勾上之后，就算配置了睡眠时间，这一个会话在睡眠时段照样正常对话；别的会话不受影响。终端配置界面和控制台设置页里都在「概率主动回复」下面一行。

- WebUI：聊天正文里选中文字再右键，弹出解释 / 翻译 / 引用追问 / 搜索 / 复制菜单；解释与翻译走「旁路请求 → 划词解释 / 翻译」档位模型，结果只显示在浮窗里，不进对话与记忆。

- 会话沙盒多了「只锁写」这一档：`/sandbox <路径> --allow-read`（命令行 `miyu session sandbox <会话> <目录> --allow-read` 同样）。她只能往这个目录里写，读哪儿都行——适合让她参考别的仓库、别的文档，但只许在当前项目里落笔。不加这个开关还是原来的读写都锁。会话列表里这种会话标着「只锁写」。
  用之前要知道：读放开之后 `~/.ssh` 的私钥、`~/.miyu` 里的 API key 她都读得到，而沙盒管不了网络——这一档防的是误写，不防提示注入，输入来源不可信的时候别用。开关在绑定那一刻定，`/sandbox clear` 解绑后归零，重绑要再给一次。

### 修复

- 命令行工具（`run_command`）的前台时长重定：**默认 30 秒 → 120 秒、上限 120 秒 → 600 秒**。此前请求值会被静默砍到 120 秒，超时又只丢回 tokio 的原话 `deadline has elapsed`——不说工具、不说实际给了多少秒、不说长活该怎么办，模型只能换着数字反复撞。现在超时会把三件事写全：`command timed out after 600s (requested 9000s, foreground cap 600s). Use background=true for longer work.`，工具描述里也补回了范围与默认值。更长的活儿照旧走 `background=true`。

- 群聊里她发过言之后的一段时间内（新配置项「群聊发完消息后观察窗口」，默认 30 秒），**任何人**的消息都会来一次「要不要接话」的判断——模拟人发完话大概率会看到后续。这一路是给判断**加分**（新配置项「群聊发完消息后观察窗口加分」，默认 0.15），她刚发完言这段时间更容易接上话；表情包、贴图这类只有 `[思索]` 之类占位符的消息不参与判断，不会因为刚说过话就变话密。daemon 日志里会写出「刚说过话加分」这一行。

- 空会话大厅（全屏 TUI 刚打开、还没说第一句话那一屏）的输入框：打字超出框宽折行之后，光标不再跑到框右边的星空里，而是跟着落到第二行框内；上下方向键也按框里真实的行走。删字让输入区从两行缩回一行时，不再一闪而过地出现第二条 footer（模式 · 模型 · 用量那一行）。拖动窗口改大小同理。

- 控制台切换会话时那个转圈不再闪。小会话几十毫秒就载完了，以前遮罩和圆环画出来又立刻撤掉，看着像闪了一下；现在要等 220 毫秒还没载完才淡入。

- 全屏 TUI 里点开一个后台命令，日志上面会先原样写出**它在跑哪条命令**。以前标题栏只有一个十来个字的短标签，看不出真正在跑什么。
- 设置里去掉了「上下文到达上限后」这一项，一律走压缩。手改过配置文件选 `pop` 的仍然照旧生效，那条代码路径没动。

- 配置界面按 q 退出时，有些改动不会弹「要不要保存」，直接就丢了。记忆相关的设置存在新旧两个位置，旧位置那份不参与比对，于是改了也被当成没改。现在按「保存下去会写成什么」来判断。

- zsh 下说话带上通配符不再被吞掉（和 fish 那条同一个毛病）。像「输出这段命令 sudo rm -f /var/.../core.*.zst」这样的一句话，zsh 会先展开通配符、匹配不上就直接报「no matches found」，这句话根本到不了 Miyu 手里。现在回车时先看第一个词：是个普通词、又不是任何命令，就交给 Miyu。真命令照旧归 zsh。装过 zsh 集成的要重跑一次 `miyu zsh-init`。

- 划词「解释 / 翻译」不再为了找一条回合把整个会话读进内存。会话越长这一步越慢，现在按 id 直接取那一条。

- 划词「解释 / 翻译」不再半路夭折。以前浮窗没钉住时，在它外面点一下就把正在进行的请求掐断了，而第一句话要好几秒才出来，看着就像点了没反应。现在出结果之前浮窗不会被顺手关掉。
- 划词「解释」会按你界面的语言回答（原来跟着选中文字的语言走：中文界面里选一段英文报错，回的是英文）。解释的提示词也重写了，先说它在这段对话里指什么，再说它本身是什么，不再复述你选的那句。
- 划词浮窗改版：等待时是和主页同款的转圈；模型的思考过程会流式显示，只占四行，想完自动收起，点一下能展开回看；复制、重试、钉住收成右上角的图标，去掉了「转成追问」；右键菜单里的「引用追问」改叫「引用」，菜单字号和正文一致。
- 输入框下方的「上下文」三个字在窄窗口下会被折成两行，已修。

- 「命令输出显示行数」现在真的管全屏 TUI 了。以前这个设置只对 shell 无缝对话生效，全屏里命令输出一律写死六行；现在两边同一个量，设 0 就是一行预览都不留。默认值是 10，想回到以前的六行把它改成 6 即可。

- 设置里「重开 REPL 回放的轮数」改叫「重开 TUI 回放的轮数」——它管的一直是全屏 TUI 重开时回放几轮。配置键没变，老配置照读。

- 终端里说话带上通配符不再被 fish 吞掉。以前像「输出这段命令 sudo rm -f /var/.../core.*.zst」这样的一句话，fish 会先展开通配符、匹配不上就直接报「未找到通配符的匹配项」，这句话根本到不了 Miyu 手里。现在回车时先看第一个词：是个普通词、又不是任何命令，就直接交给 Miyu。真命令照旧归 fish，`rm -f /nope/*.zst` 该报错还是报错。（bash 不受影响；zsh 仍有同样的问题。）

- 控制台的表格里可以用 `<br>` 换行了。`<br>`、`<br/>`、`<br />` 都认，大小写不限。行内代码和代码块里的 `<br>` 仍是原样文字；带属性的 `<br class=x>` 和其他 HTML 标签照旧当纯文本，不会被当成标签执行。

- `miyu pm` 暂时从帮助和补全里隐藏。包管理的接口还没定稿，先不公开推荐。已经装好的包照常能用，显式敲 `miyu pm …` 或 `miyupm …` 行为一点没变。

- 子代理浮层收起成 `Worked for …` 之后，点开它能看到的东西不再比收起之前少：以前收段那一刻会丢掉露在每一步抬头底下的内容，而且每一步的展开状态会被重置（点开着的当场合上）。

- 后台子代理的思考在任务面板里不再一卡十几秒。以前它只有「遇到空行」和「攒够 600 字符」两种落盘时机，而模型分析、权衡、列条件的时候中间往往不空行——实测面板会整整 **12 秒**不动一下，然后一大段突然冒出来。现在攒够 300 毫秒也落一条：实测同一段思考从「12 秒一跳」变成「约 0.35 秒一跳」。

- 后台任务面板里子代理的思考现在是逐字跟着走的。以前它每 150 毫秒重读一遍任务日志，而日志是按自然段落写的，所以面板总是一段一段地跳；现在面板直接订阅子代理的原始进度流（日志那条路留作退路，daemon 重启过或看老任务时照旧能用）。

- 面板里「已思考」那一行改成和主线一致的 `已思考 · 1.2s`——报耗时，不再把思考正文的尾巴挂在抬头上（想了什么点开看）。另外老日志里认不出的工具，图标从旧齿轮换成和主线一致的芯片。

- 子代理浮层现在也跟着显示开关走。以前浮层只认「是不是全屏」：不管开关设成什么，浮层里的思考和工具详情永远是收着的、得一个个点开。现在「展开」那一档下，浮层里那几步出来就是展开的。（后台任务面板还没跟上——它是从日志读的，手上拿不到这两个设置。）

- 工具那一档不再变成两种版式叠在一起。以前把「显示工具调用信息」调成详细，全屏 TUI 下命令走时间线、别的工具打旧版的「工具 x / 参数 … / 结果 x ok / 输出 …」卡片，而且那些工具**根本不进时间线**——`Worked for` 也数不到它们；非全屏（shellhook / 单次命令）下更干脆：整条时间线直接消失，退回那套旧卡片。现在不管哪个形态、哪个档位，工具都落成时间线里的一步。顺带修掉三处重复输出：子代理转后台的提示、子代理跑完的用量、以及工具自报的进度，在那一档下原来会同时出现在时间线和屏幕上两遍。

- 思考那一档不再是一段脱离时间线的裸绿字。以前把「显示思考过程」调成详细，全屏 TUI 会直接把思考正文流在屏上——没有竖线、不占时间线上的一步、`Worked for` 也数不到它。现在它落成时间线里的「已思考」那一步，和别的步骤一个样子。

- 后台任务面板里 `Worked for … · N tools` 报的工具数比真跑过的多一个。多出来的那个正是 AI 汇报「工具调用 N 次」的那一行本身——它被当成了一次工具调用数了进去。

- 子代理**加载工具/技能**时，后台任务面板里那一步会出现两遍——一遍没有状态和耗时，一遍带着，而且点开看不到工具输出。原因是面板判断「这一步是不是一次工具调用」看的是它的图标，而 `load_tools`、`manage_skill`、`load_skill` 用的图标正好和面板里「交给它的差事」那一行是同一个，于是被判成「不是工具调用」：结果配不上它只能自己另起一行，工具输出也因为找不到归属被丢掉。现在这一步是什么由事件本身决定，和图标无关。

- 「显示工具调用信息」设成 `full` 时，每起一个子代理，屏幕上会多出一行 `进度 子代理: __subagent_brief__{"description":…,"prompt":…}` 这样的内部数据。现在这类内部标记一律不再打到屏幕上。

- 没装 Nerd Font 的人（`MIYU_TUI_ASCII=1`）在子代理面板里看到的图标是**混的**：工具那几个正常退回 `⚙`，而「已思考」「跑砸了」「提示词」三行仍然是方框。现在这三处也跟着退，面板里不会再一半方框一半正常。

- 工具名补齐中文：QQ 相关的 13 个工具此前在中文界面显示英文（发送消息、群活跃榜、查询群成员、QQ 头像、后台任务、词元用量等），现在都有中文名。反过来，英文界面下也不再冒出中文名——没写英文名的脚本会按工具名折一个（`xhs_search` → `Xhs search`）。另外 `edit`、`grep`、`read`、`kb`、`artifact` 五个工具在控制台里的名字改得和终端里一致了（此前同一个工具两处显示不同名字）。

- **显示设置换成了三个开关**：`展开思考内容`、`展开工具内容`、`过程收起成 Worked for`。原来是两个三值档（隐藏 / 摘要 / 完整）加一个「不自动收起过程」，一个下拉里装着两件不相干的事——「出不出现」和「出来是展开还是收起」，而且「完整」那一档还顺手换掉了整条渲染路径。现在三件事各管各的：

  - **展开思考内容 / 展开工具内容**：开着的话，那一步出来**就是展开的**，不用点；再点一次照样收回去。**还在想、还在跑的时候就是展开的**，不用等它结束。关着就只有抬头，点开才看。以前这一档是把内容挂在抬头底下当一截预览——那一行明明还能点，点开看到的又是几乎同一份内容。
  - **过程收起成 Worked for**：只管收不收段。关掉的话每一步就地留着（和 shell 无缝对话那条路一个样子），**步骤照样点得开、详情照样收在块里**；以前关掉它会顺手把那些步变成点不开、正文铺一地。
  - 没有登记处可点的形态（shellhook、单次命令）上，「展开」= 正文就地印在抬头底下，「收起」= 只有抬头。

  旧配置自动折算，不用手改；「隐藏」那一档已经删掉，配过它的会落到「显示 + 收起」。

- 子代理浮层里的正文不再一个词一行。后台面板现在直接订子代理的原始进度流（上一条说的那个改动），而那条流是**逐 token** 的——面板把每一条当成一行渲染，于是一句话被排成了一串台阶。现在换行只看内容里真正的换行，跟这条流切在哪儿无关。

- **浮层里的字现在选得动、复制得走了。** 子代理面板、后台任务面板、后台命令面板——它们装的正是最想复制走的东西（子代理的输出、命令的日志），而在此之前浮层整个不做选区：按下和拖动都被直接吞掉。现在和正文那侧一个规矩：拖过是选区复制，原地点一下还是开合那一块；左边的缩进不进剪贴板，粘出去不用再手动删一遍。

- 后台任务面板里，子代理**开过口之后**，后面每一步点开都会在下一帧自己合上。块的编号是按位置记的，而正文段不占编号，于是它后面所有步骤每一帧都在换编号——点开的那一块下一帧就对不上了。顺带还把可展开内容的登记处当垃圾场用（每帧几十块），几秒钟就把主线上那些能点开的行一起挤掉。

- 后台任务面板里子代理的「已思考」重新带上耗时。面板 09-17 起改订子代理的原始进度流（更实时），而那条流里一点时间信息都没有——同一段思考，换条路看就成了光秃秃的「已思考」。现在由发的那一侧在每段思考结束时报一次耗时。

- 后台任务面板里「准备执行」会攒成一屏。参数是逐块流的，每块都报一次「准备执行」——它只有作为**此刻**那一条时才算数，别处的都是过去式。末尾那道过滤一直在，但一段过程收成 `Worked for …` 时那些准备行是被**卷进收缩行肚子里**的，过滤扫不到：点开收缩行就是一串「准备执行」，而且它们还被算进了工具次数——你那份真机日志里 2 次工具报成了 **17 tools**。

- 后台任务面板偶尔**整条时间线消失**、日志原样铺出来。面板判断「这份日志有没有步」靠的是一份手抄的标签清单，抄漏了 `[准备]`，后来又漏了新加的两个续接标签——只落了这几种标签的那一截日志（子代理刚起头就在准备工具，或者读到的尾巴全是续接）会被判成「没有步」。现在直接用协议里那份清单，不再手抄。

- 后台任务面板现在也跟着「展开思考内容 / 展开工具内容」走了。它是从日志读的，手上一直没有配置入口，于是同一件事在子代理浮层里跟着开关走、在后台任务面板里永远收着。

- 浮层里的 tag 行现在有悬浮提亮了——鼠标扫过哪一块，那一块整个亮起来。以前面板把鼠标移动和别的事件一起吞掉了，「哪儿能点」全靠猜（正文那侧一直是有的）。

- 子代理的**编辑文件**那一步现在有 diff 了：抬头上带 `+3 -1`，点开是渲染好的 diff。以前后台任务面板这两样都没有——子代理内层的编辑拿不到工具自己算的那份真 diff，只有调用参数里的补丁信封，而那份信封一直没被用上。

- 「过程收起成 Worked for」现在也管得住子代理浮层和后台任务面板了。这两块面板以前是无条件收段的，那个开关对它们等于不存在。

- 子代理面板里**命令那一步**和主线终于长一个样了：抬头给 title（不是命令全文），命令露在抬头底下、点开看全部。以前后台这条路把命令全文当窥视塞进抬头，同一步在主线和面板上两副面孔。模型没给 title 的话抬头仍退回命令本身——那儿是唯一的落点，空着比长一点更糟。

- 命令预览那几行跟着抬头一起变暗了。以前抬头是暗色、底下的命令是正常文字色，附注比把手还显眼。点开看到的那份仍是正常色——那儿它才是内容。

- 浮层里编辑那一步的 `+3 -1` 现在是绿加红减，和主线一致（之前是白的）。

- 子代理面板里命令那一步终于和主线**逐处**一致了：抬头 `运行命令 · 1.2s · <title>`（跑着的时候计时也在走），命令露在抬头底下，点开时整块换掉——不再出现「展开之后命令有两份」「收起成 Worked for 再点开命令预览就没了」。根因是面板自己拼了一份「一步占哪几行」，尾巴落在可点区域外面；现在和主线用同一个函数。

- 展开命令那一步时，命令正文也跟着变暗了（之前只改了露在抬头底下那几行）。同一块里最显眼的该是它吐了什么，不是我让它跑了什么。

- 子代理面板里每个工具都有耗时了。原来掐表的只有写日志那一侧，而面板 09-17 起改订原始进度流，那条流里一点时间戳都没有——于是抬头上只剩工具名和窥视。现在由发的那一侧在结果里带上耗时。

- 终端里**长任务（`/goal`）跑着的时候，输入框右上角有一行常驻提示**了：`/goal running · 第 3 轮 · 12s`。以前目标一旦开跑，屏幕上就只剩正文，看不出「它还在自己往前跑吗、跑到第几轮了、这一轮跑了多久」——只能反复敲 `/goal` 去问。这行提示只说这三件事，秒数每秒自己走；停下来的时候写 `paused`、不再显示秒数（秒数停着不动反而像卡住了）。**「停下来」包含三种情况**：你暂停了、被打断过没重新武装、以及**上一轮她一个工具都没调、驱动器停下来等你开口**——最后这种以前完全看不出来，屏幕上没有任何动静，`/goal` 却说「进行中」。受阻时写 `blocked`。没有目标、目标已完成就什么都不画，窄终端放不下整条也不画（截断出来的半句比没有更糟）。颜色用的是当前模式的高亮色，和输入框左侧那根粗线、左下角的模式标签是同一个（普通蓝、开发酒红）。

- 空会话里用 `/goal` 建目标**现在会退出大厅**。大厅那层星空是盖在正文之上的，而建完目标续轮马上就开始往屏幕上写字——以前那些字全被星空糊住，看上去像「敲了 `/goal` 什么都没发生」，其实跑完了只是看不见。从 WebUI 建的目标、空会话里后台任务的跟进回复也是同一个毛病，一并修了。

- 续轮跑着的时候敲 `/goal <另一个目标>`，**被拒会说话了**。那会儿的 `/goal` 走的是「不打断流、静默执行」那条路——它敢静默是因为 `pause`/`clear`/`edit` 都会把正在跑的那一轮掐断，中断本身就是反馈；而「本会话已有未完成的目标」这类被拒的命令流照跑、状态也不变，于是一点反应都没有。现在被拒会弹一句提示告诉你该 `/goal edit` 还是 `/goal clear`，成功的仍然静默。

- **在 [herdr](https://herdr.dev/) 里开 Miyu，侧栏会显示她在干什么**。herdr 是个「知道 agent 在干嘛」的终端多路复用器，每个 pane 的 agent 状态往 tab 和 workspace 上卷。现在 Miyu 会主动把自己的状态报上去：等你打字是空闲、在想和跑工具是「进行中」、**她反问你一句时整条 workspace 变红**（herdr 的「卡住等人」状态），你答完自动变回进行中。报上去之后 herdr 自己的桌面通知、`herdr agent attach / wait / prompt` 三条命令都能对着 Miyu 用。覆盖 TUI、`/goal` 续轮、后台任务唤醒的跟进回合，以及 shell 无缝对话那条路（一次性进程跑完把 pane 还回去，不留一个已经不存在的 agent 赖在侧栏上）。**不在 herdr 里就完全不做事**——判据是 herdr 自己注入 pane 的那几个环境变量，缺一个都不起进程；上报是异步的，失败一声不吭，不会拖慢回合也不会把错误糊到画面上。
  - 顺带两件：在 herdr 的 pane 里，**终端标题会跟着写成当前会话的标题**（只有交互式 TUI 会写，一次性命令不动你的标签页名）；**图片也能正常显示了**——herdr 把 `TERM` 降级成 `xterm-256color`，Miyu 原来据此判定「这不是 kitty」，于是明明能显示图片的终端里退回了字符画。现在会去读 herdr 自己的配置（`terminal.kitty_graphics`），它开着就照样走 kitty 图形协议。

- **`miyu` 和 `miyu dev` 现在打开的是一条新会话**，不再接着上次那条聊。敲 `miyu` 想要的多半是一张白纸；要接着上次聊用 `/session` 挑那条会话。指针指的那条**本来就是空的**就原地用，所以反复开关 `miyu` 不会攒一堆空会话。**上键调历史不受影响**——输入历史是按会话存的，开新会话后会自动从上一条 REPL 会话接过来，不会一按上键就是空的。会话里的 `/dev` 和 `/normal` **保持原样**：仍然是「切到那条车道最近用的会话」。另外新增 **`/dev new` 和 `/normal new`**，效果和 `miyu dev` / `miyu` 启动完全一样——去那条车道开一条新会话（那条车道当前会话是空的就原地用）；已经在那条车道上时也照做。这两条只认 `new` 这一个参数，打别的会提示一句，不会默默开新会话。⚠️ 要重启 daemon。

- **她说话的时候，斜杠命令不再一律失灵**。以前回合一开始跑，敲任何命令按回车都没有反应——不执行、也不说一句为什么，连 `/new` 都做不到。现在按「这条命令回合中能不能做」分三类：
  - **能做的直接做**：`/new` `/session` `/dev` `/normal` `/help` `/usage` `/history` `/models` `/persona` `/config` `/effort` `/rename` `/sandbox` `/stt` `/exit` 都能在她说话的时候用。做法是先把这一轮**放到后台继续跑**（她不会被打断），执行完命令再挂回来接着看——已经看过的那半截不会重播一遍。代价是正文上会留一道接缝：命令执行的位置会断一下再续上。换走了会话的（`/new` `/session` `/dev` `/normal`）就不挂回来了，那一轮在后台跑完，去 `/session` 切回去能看到完整结果。
  - **`/goal` 照旧**：它完全不往屏幕上写，跟随一点都不断。
  - **做不了的会说一句为什么**：`/undo` `/pop` `/compact` `/reset` 要改写正在被续写的那段对话，`/delete` `/wipe` 会把这一轮掐掉，`/clear` 在正文还往屏幕上写的时候既清不干净也保不住上文，`/reset-memory` 撞上她正在写记忆。这些会在右上角说明原因，**你打的字原样留在输入框里**，等她说完再按一次回车就执行，不用重打。
  - 顺带修一个一直都在、以前碰不到的毛病：**换会话时，正在跑的那一轮不会从屏幕上消失了**。换会话会清掉正文再按历史重画，而「正在跑」的那一轮还没落库、不在历史里，于是它连同你刚说的那句话整个不见了。现在切进一条有回合在跑的会话，会自动挂上去从头跟着看。
  - 直连模式（`MIYU_DIRECT=1`）没有后台守护进程，做不了「放到后台再挂回来」，命令仍然要等这一轮说完；但现在会明确告诉你这件事，不再是静默无反应。

- **`/session` 菜单里按 Ctrl+D 直接删，不再问「y/N」**。删掉的那一行从列表里消失就是回执，光标停在原位（下面的行顶上来），所以可以连着按连着删。删**当前**会话（带 `*` 那条）同样不问——但它会**当场收掉菜单**并落到一条空会话上，不再让你站在一条已经不存在的会话的画面上继续挑（以前会看见被删会话的正文，一回车还会跳进「终端集成会话」）。`/delete` 命令的确认保留：那是打着名字删的另一条路。⚠️ 删了就是删了，没有撤销。
  - 顺带两件：**「终端集成会话」不再出现在 `/session` 列表里**。它是 shell 无缝对话那条路的会话，终端 REPL 本来就进不去（切过去会被当成「指针失效」自动换一条新的），列出来只会让人误选；要用它还是走 shell 那条路或 `miyu session`。`/session <编号>` 的编号跟着同一份列表，不会错位。
  - **`/new` 在空会话里不再新建，只会告诉你「已经是一条新会话了」**；带名字（`/new 某某`）则把当前这条改名。以前 `/new` 是唯一会攒空会话的地方——连敲两次不说话就多两条「新会话」。现在规则和 `miyu` 启动、`/dev new` 统一：**空会话永远只有一条**。当前会话已经说过话时，`/new` 照旧新建。

- **她反问你的时候，herdr 侧栏那条红色状态在你答完之后会正常恢复**。以前只在她提问时报了「卡住等人」，答完没报回「进行中」，于是侧栏一直红到整轮结束——人在别的 pane 干活时会以为她还在等。另外 **`/goal` 续轮和后台任务唤醒的回合里她提问，侧栏原来完全不变红**，这条路一个状态都没报，现在补上了。（只影响在 herdr 里跑的场合，不在 herdr 里一切照旧。）

- **她说话的时候开 `/models` / `/session` 这类面板，回合要是在面板开着的时候说完了，正文的后半截不会再丢**。以前收掉面板重新挂回去时，Miyu 核心一看「这一轮已经不在进行中」就直接回绝，客户端悄悄放弃——屏幕上就停在你开面板那一刻，看着像回复被截断了（其实那一轮在核心里好好跑完了，历史里是完整的）。现在这种「回来晚了」的情况会把缓冲里剩下的内容补完再收尾。⚠️ 要重启 daemon。

- **她反问你的时候，上面那几行过程不再消失了**。以前她提完问、面板弹出来的那一刻，「已思考 · 334 词元 · 3.4s」「准备问题 · 644ms」这些行会整片从屏幕上消失，等你答完面板收起才重新冒出来——看着像刚才的输出被吞了。原因是面板让屏时把还在进行中的那几行**抹掉**了；给终端图片让屏那条路一直是对的（只让位、不抹），现在面板这条路也改成一样。她提问前说过正文的场合本来就没这个毛病（正文开头会把过程行定稿落盘），所以只在「思考完直接提问」时看得到。

- **空会话里按住退格（或任何键不放）时，大厅的星空和扫光不再定格**。以前那段动画只在「这一拍没人按键」时才往前走，而按住键时按键来得比一拍（40 毫秒）还密，于是一帧都推不出去，看着像卡死了；松手立刻又动起来。现在动画按**时刻**走，打字期间照样往前推。

- **Antigravity 的内容策略拦下一句话之后，整条会话不会再跟着哑掉**。以前被拦的那一轮会留在上下文里，之后每问一句都会把同一段话连同新消息一起发过去、再被拦一次——群聊里尤其难受：那边的内部错误是不往群里发的，看上去就是她从某一刻起再也不说话了，除非手动清上下文或换会话。现在这一轮会**从后续上下文里摘掉**，下一句照常能回；报错里也多一句「这一轮已不再进入后续上下文」，省得你猜要不要手动清。⚠️ 只针对 Antigravity：它的拦截是会话级粘性的。别的供应商的内容策略、以及限流和网络抖动，都不会让消息被摘掉。⚠️ 被摘掉的那一轮在 `/history` 和终端回放里也不再出现（数据还在库里）。⚠️ 要重启 daemon。
