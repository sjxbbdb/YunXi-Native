# TUI 布局与高把握 bug 后续验收

本轮用户要求：已验收通知合并 main；重新实现会话列表位置；检查仓库 todolist.md
和 Downloads/Miyu-2026-09-14-plan 中能确定根因的 bug，修复后提供 debug 验收。

## 已合并

通知在回复期间过期的修复：独立提交 `6a6dd483`，main 合并 `f08bc80f`。
已写入 next-release-note.md。此前 SSH footer 修复已在 main，用户再次确认正常。
本次保留主仓库 AGENTS.md、todolist.md 的用户本地修改。

## 本轮已验收（2026-09-16）

1. `/session`：空会话保留大厅，列表放在模式与快捷提示下面，并对齐输入框。
   非空会话按实际面板高度为正文让出空间，正文仍能翻页。
2. 选择当前会话不再重载，保留阅读位置。
3. 配置 TUI 收到窗口缩放事件后重绘，编辑草稿保留；缩放不能充当“按任意键”。
4. MCP `isError: true` 映射到既有 `ok:false` 工具失败格式，保留诊断内容。
   成功结果的输出字节不变，不修改工具描述、目录或历史回放。
5. QQ 覆盖窗口：验证并修复旧承诺仍在排队、尚无运行回合时的失效与归属清理。
   不把窗口改成防抖，不保证已经发出的旧模型请求从未发生。

## 启动

```bash
MIYU_HOME=/home/shorin/Documents/github/Miyu-tui-quick-2026-09-15/target/manual-tui-home \
MIYU_TUI=1 \
/home/shorin/Documents/github/Miyu-tui-quick-2026-09-15/target/debug/miyu
```

该 home 沿用上轮测试配置。本轮不重置用户已产生的测试会话，不连接生产平台。
最终 debug 构建于 2026-09-16，保留调试信息，自报版本 `miyu 0.6.0`。
SHA-256：`9662983b77c2ece8ac52438c7b94b548caf9ca9bd2c0437497484d9cd563fc24`。

## 人工检查

1. 新建空会话，打开 `/session`。大厅、输入框、模式与设置提示保留，列表出现在
   下方星空区域。搜索、上下选择、Esc 返回正常，缩放窗口后重新布局。
2. 让 AI 输出超过一屏的正文，再打开 `/session`。最新正文在列表上方可见，正文与
   列表有间隔；PgUp/PgDn 可以回翻，Esc 后恢复输入框。缩小再放大窗口重复检查。
3. 回翻到正文前部，打开列表并选中当前会话：阅读位置不跳回末尾，不出现切换提示。
4. 打开 `/config`，在菜单和文字编辑框分别缩小、放大窗口，不额外按键也应重绘。
   编辑内容不丢，取消编辑不保存。错误提示界面缩放后仍停留，真实按键才返回。
5. 已接入 MCP 时，调用一个会明确失败的测试工具，应显示工具失败并保留原报错。
   本轮另有本地假 MCP 自动回归，无需依赖外部服务证明错误标志转换。
6. QQ 在独立测试配置/测试会话验证：同一发送者在覆盖窗口内补发更正，旧排队
   任务不再随后单独作答；窗口外不同问题仍独立处理。真实群的所有重复场景并未
   都证明由这一条路径引起，不把本修复称为全链路问题已消失。

## 筛查边界

- 命令输出流式：前台展开/后台浮层在 daemon 和 direct 四个场景均通过旧二进制
  PTY，未发现可证明的现存根因，未猜改。用户原命令可能需要额外现场复现。
- todo 截断：原窄屏长表/多批次/展开回翻未稳定复现，暂不改。
- QQ 同消息正文重复（F6）：缺原始 stream-json，未把计划中的假设当确定根因。
- QQ debug 慢：已知长图渲染有明显 debug 开销，但尚未确认现场耗时阶段。
- `rm -f`：现有默认拒绝规则并非禁止所有 rm 操作，不能未经明确语义变成全禁。
- F3/F4 已有独立工作区未提交实现，本批避免重叠修改。
- 配置退出保存提醒、删除“上下文到达上限后”等条目已在 main 的既有修复中。
- 新代理层级、只读模式、扩展接口等是功能/架构专项，不当作小 bug 顺手实现。

## 自动验证记录

- CLI：281 项通过，含新增面板与大厅空间布局用例。
- config_tui：53 项通过，1 项原有 ignored。
- MCP：12 项通过；新增错误转换回归修前 4 条失败、1 条通过。
- real_context：52 项通过；新增归属回归修前 5 条失败、1 条通过。
- 配置 resize 真 PTY：菜单、编辑文字/光标、普通提示和 ERROR 提示全部通过，
  产物 `/tmp/miyu-config-resize-gqb2rngt`。
- 最终 `/session` PTY：大厅保留与对齐、正文空间/滚动、缩放、搜索、删除确认与
  真正删除/切回、列表与显式参数两条当前会话 no-op、选择器内通知到期全部通过。
  产物 `/tmp/miyu-session-picker-6vj1z6vy/out`。
- 最终提问面板 PTY：正文、间隔、滚动、切题、缩放和回答通过。
  产物 `/tmp/miyu-question-body-d2q_t6gs/out`。
- `cargo fmt --check`、`git diff --check`、依赖方向检查通过。
- 文件规模检查保留原有失败：`src/render/tests/timeline.rs` 的基线为 1517 行、
  当前 1564 行。已验证该文件与 main 字节一致，本轮未改，未放宽基线。

用户已确认本批全部成功，授权合并 main 并清理已完成 todo。独立提交：

- `42042d9e`：会话列表布局与当前会话免重载。
- `d53b5e54`：配置 TUI 缩放重绘。
- `4a0a31d2`：MCP 工具错误标志。
- `0a813b4a`：QQ 覆盖窗口排队归属。

next release note 已逐项记录；发布与推送另行执行。
